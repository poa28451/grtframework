package com.googlecode.grtframework.sensor.core;

/**
 * A FilteredSensor republishes events from another Sensor after performing some
 * signal processing
 * 
 * @author ajc
 * 
 */
public interface FilteredSensor {
	/*
	 * Unclear if this should be a class or an interface.
	 * 
	 * Class:
	 * 
	 * ability to implement some event framework(although it may be better to
	 * just use the same event interface), and history
	 * 
	 * 
	 * Interface:
	 */


}
